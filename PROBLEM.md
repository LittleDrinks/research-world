# 自主研究的审查带宽
## 问题
研究 Agent 能在数天内产生远超人类逐条审查能力的候选、实验和争论。线性对话把规划、证据、执行、错误和修订混在一起；运行越久，阅读成本和上下文噪声越高。研究者无法快速回答：这段时间发现了什么、尝试了哪些不同方向、失败在哪里、下一步做什么、上游错误影响了哪些结论。
长对话也不是可靠记忆。无关历史挤占推理空间，错误经验会锁死搜索，完整披露会诱导局部修补；直接丢弃又会遗漏条件。Agent 的生成速度把任何延迟审核放大成下游污染，事后阅读几十 MB 轨迹不能成为控制手段。
## 使用场景
一个集群只推进一个科学问题。多个 Agent 并行提出行动、执行实验、复现结果和独立审核；人类负责处理冲突、不确定结果与高风险裁决。125 个问题用于广度与 OOD 覆盖，5 个选题用于深度演示；不同问题不共享一张研究图。
研究者先看到一个时间窗口内的增量：新增候选方向、已完成尝试、被否定路线、待执行行动和异常。每项内容可逐层展开到依赖、原始证据、代码、环境与审核记录；质疑上游内容时，系统给出受影响的后续工作。
## 核心矛盾
- 研究吞吐需要继续增长，人工审查负担不能随轨迹长度线性增长。
- 持久信息必须足够支持复现和追责，又不能把未审核噪声传播给后续 Agent。
- 历史需要防止重复错误，又不能默认约束独立搜索。
- 研究结果允许不确定和冲突，系统不能把审核伪装成自动发现真理。
- 证据形态从文本、文献和代码扩展到图像、表格、日志、仪器读数与湿实验记录，保留强度取决于可复现成本。
## 产品边界
- 图谱是唯一持久研究状态；对话通过审核并沉淀后默认删除。
- 入图发生在行动或结果被后续工作依赖之前；待审内容不能继续生长依赖路径。
- 规划、执行、审核和评分分离；执行 Agent 不持有隐藏目标或自报最终裁决。
- 原始轨迹不默认进入研究图谱。计算实验保留代码、输入、配置、环境、随机种子与不可变产物；难以重跑的实验保留完整证据。
- 不建立全局方法 taxonomy 或固定失败类型。机制关系是带来源、可冲突的局部判断；审查视图从图谱按需派生。
- 不把 125 个问题混成一张图，不把界面节点数当作阅读成本，不以单一总分定义研究价值。
## 设计原则
最大化研究员对 agent 系统的掌控：可读性（图谱审查视图）、可玩性（能力装配）、便捷性（领域内置），服务对象是不同领域的研究员。
## 成功条件
- 研究者无需阅读完整轨迹即可正确说明本周期新增方向、关键失败、待办和影响范围，并能回到支撑证据。
- 任一入图内容都能说明由谁、在何种活动中、使用什么输入产生，经过何种审核，依赖哪些内容。
- 已审核信息在删除对话后仍足以继续规划、独立复现和质疑；未审核内容不会污染默认检索。
- 错误上游内容被否定时，依赖结果可确定性定位并停止继续生长，而不是依赖模型重新阅读历史猜测。
- 研究成本同时报告机制覆盖、执行有效性、审核冲突、人工时间、计算与 Token；参数微调不能冒充新方向。
## 未决问题
- 低置信度图谱查询是否自动触发一次有预算上限的外部检索，或必须由人确认。
- 真人在原始轨迹、图谱审查视图、图谱加独立 Agent 预审三种条件下的正确率、时间和盲从风险。
- 计算实验跨硬件复现差异与湿实验不可重复结果的升级裁决边界。
