[project]
name = "research-kernel-contract"
version = "0.1.0"
description = "Public values for the Research Kernel interface"
requires-python = ">=3.12,<3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["kernel_contract"]
