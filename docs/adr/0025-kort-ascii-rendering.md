# Kort 字符雕塑：统一 3D 点云投影
卡片、地球、特性图标与 Why 主视觉统一为 3D 点云，经透视投影、字符栅格 z-buffer、深度着色与垂直残影生成二进制雕塑。形体序列直接写在画布 `data-shapes`：卡片承担翻页式 morph，地球保留大陆密度，人像、行星、DNA 与层叠结构保持独立轮廓，Why 主视觉在圆环、苹果、球体间 morph。近景用深紫实色，远景用低透明浅紫；轮廓内部保留足够字符密度，禁止退回二维 SDF 填充。Hero 使用稀疏金融字形马赛克。单一 `requestAnimationFrame` 调度，可见区域渲染；无外部依赖与图片资源。
